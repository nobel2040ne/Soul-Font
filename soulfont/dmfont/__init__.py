"""Upstream DM-Font training code, kept as vendored a copy as possible.

Not used by the web app; it is here so the project root is not littered with it.
These modules import the root-level `models`, `datasets` and `utils` packages, so
train.py puts the project root on sys.path before importing them.
"""
